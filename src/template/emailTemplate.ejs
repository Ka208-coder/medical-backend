<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>Admin Report</title>
  <style>
    body { font-family: Arial, sans-serif; color: #333; }
    .container { padding: 20px; border: 1px solid #ddd; border-radius: 10px; }
    h2 { color: #007bff; }
    p { margin-bottom: 10px; }
  </style>
</head>
<body>
  <div class="container">
    <h2>📢 Admin Report</h2>
    <p>Hello Admin,</p>
    <p>Here’s the latest data report:</p>

    <ul>
      <% data.forEach(item => { %>
        <li><strong><%= item.title %></strong>: <%= item.value %></li>
      <% }) %>
    </ul>

    <p>Generated at: <%= new Date().toLocaleString() %></p>
  </div>
</body>
</html>
